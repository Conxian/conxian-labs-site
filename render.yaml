services:
  - type: web
    name: conxian-labs-site
    env: node
    region: oregon
    buildCommand: "npm install"
    startCommand: "npm start"
    plan: starter
    pullRequestPreviewsEnabled: yes
    autoDeploy: yes

